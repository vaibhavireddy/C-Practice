//WAP to print negative if the given number is lessthan 0
#include <stdio.h>
int main()
{
    int a;
    printf("Enter number:");
    scanf("%d",&a);
    if(a<0)
    { 
      printf("negative");
      }
      return 0;
      }
