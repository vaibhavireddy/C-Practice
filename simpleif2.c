//write a program to print positive if given number is greater than 0
#include <stdio.h>
int main()
  { 
     int a;
      printf("Enter number:");
      scanf("%d",&a);
      if(a>0)
      {
        printf("positive");
        }
        return 0;
        }
